swagger: "2.0"
info:
  title: Messages API
  version: 1.0.0
schemes:
- http
produces:
- application/json
consumes:
- application/json
paths:
  /buckets/{bucketKey}/messages:
    delete:
      summary: Clear a bucket (remove all messages).
      description: ""
      operationId: ""
      parameters:
      - in: path
        name: bucketKey
        description: Unique identifier for a bucket
      responses:
        200:
          description: OK
      tags:
      - ""
    get:
      summary: Retrieve a list of messages in a bucket
      description: ""
      operationId: ""
      parameters:
      - in: query
        name: before
        description: Only return messages before the given Unix timestamp
      - in: path
        name: bucketKey
        description: Unique identifier for a bucket
      - in: query
        name: count
        description: Maxiumum number of messages to return. Default 50, max 1000.
      - in: query
        name: since
        description: Only return messages after the given Unix timestamp
      responses:
        200:
          description: OK
      tags:
      - ""
    post:
      summary: Create a message
      description: ""
      operationId: ""
      parameters:
      - in: body
        name: NewMessage
        schema:
          $ref: '#/definitions/holder'
      responses:
        200:
          description: OK
      tags:
      - ""
  /buckets/{bucketKey}/messages/{messageId}:
    get:
      summary: Retrieve the details for a single message.
      description: ""
      operationId: ""
      parameters:
      - in: path
        name: bucketKey
        description: Unique identifier for a bucket
      - in: query
        name: messageId
        description: The unique identifier for this message
      responses:
        200:
          description: OK
      tags:
      - ""
  '/messages ':
    ' post ':
      summary: Messages
      description: ttttCreates an UI message.ttt
      operationId: -messages-
      responses:
        200:
          description: OK
      tags:
      - ""
  '/messages/{id} ':
    ' delete ':
      summary: Messages {id}
      description: ttttDeletes an existing UI message.ttt
      operationId: -messages-id-
      responses:
        200:
          description: OK
      tags:
      - ""
    ' get ':
      summary: Messages {id}
      description: ttttGets an existing UI message by Id.ttt
      operationId: -messages-id-
      responses:
        200:
          description: OK
      tags:
      - ""
    ' put ':
      summary: Messages {id}
      description: ttttUpdates an existing UI message.ttt
      operationId: -messages-id-
      responses:
        200:
          description: OK
      tags:
      - ""
  '/messages?active={active}&amp;customerId={customerId} ':
    ' get ':
      summary: Messages?active={active}&amp;customerId={customerId}
      description: ttttGets a list of UI messages. UI messages are used for user notifications
        on announcements/information/warnings.ttt
      operationId: -messagesactiveactiveampcustomeridcustomerid-
      responses:
        200:
          description: OK
      tags:
      - ""
definitions: []